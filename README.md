# Live Jellyfish

A full-screen animated jellyfish scene built with HTML5 Canvas, JavaScript, and p5.js in WebGL mode.

Open `index.html` in a browser. Move the pointer to steer the water current. The lower-right controls pause, reset, and enter fullscreen.

The page loads p5.js from jsDelivr, so an internet connection is needed the first time it runs.
